name: smart_wallet
description: A new Flutter project for tracking income and expenses.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.3.0
  path: ^1.8.3
  intl: ^0.20.2
  sqflite_common_ffi: ^2.3.0
  sqflite_common_ffi_web: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
